server:
  port: 8080

spring:
  application:
    name: smartkrishi-backend
  data:
    mongodb:
      auto-index-creation: true
      uri: ${MONGODB_URI:mongodb+srv://helperboyg_db_user:NLQ0PFaS8AmhqYnQ@cluster0.ll0hhbn.mongodb.net/smartkrishi?appName=Cluster0}
      database: smartkrishi
  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB
  mail:
    host: ${MAIL_HOST:smtp.gmail.com}
    port: ${MAIL_PORT:587}
    username: ${MAIL_USERNAME:prabinsingh9844@gmail.com}
    password: ${MAIL_PASSWORD:jcybpoiunonniouz}
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

# JWT Configuration
app:
  jwt:
    secret: ${JWT_SECRET:404E635266556A586E3272357538782F413F4428472B4B6250655368566D5970} # 256-bit key
    expiration-ms: 86400000 # 24 hours
    refresh-expiration-ms: 604800000 # 7 days

# Cloudinary Configuration
cloudinary:
  cloud-name: ${CLOUDINARY_CLOUD_NAME:cigbxzhb}
  api-key: ${CLOUDINARY_API_KEY:327782332192974}
  api-secret: ${CLOUDINARY_API_SECRET:y8RiuCZ812lB49YPz1feH98_yYQ}

# Springdoc OpenAPI Configuration
springdoc:
  api-docs:
    path: /api-docs
  swagger-ui:
    path: /swagger-ui.html
    operationsSorter: alpha
    tagsSorter: alpha

logging:
  level:
    org.springframework.data.mongodb.core.MongoTemplate: DEBUG
    org.springframework.security: INFO
